---
import '../styles/global.css';
import Navbar from '../components/Navbar.tsx';
import { ContactBento } from '../components/ContactBento.tsx';
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Contact | Freddie Philpot</title>
		<script is:inline>
			(function() {
				const theme = localStorage.getItem('theme') || 'system';
				const root = document.documentElement;
				if (theme === 'system') {
					const systemDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
					root.classList.add(systemDark ? 'dark' : 'light');
				} else {
					root.classList.add(theme);
				}
			})();
		</script>
	</head>

	<body class="bg-background text-foreground min-h-screen">
		<Navbar client:load />
		
		<main class="pt-24 px-4 max-w-5xl mx-auto pb-16">
			<h1 class="text-4xl md:text-5xl font-bold mb-4">Contact</h1>
			<p class="text-muted-foreground mb-8">Get in touch</p>
			
			<ContactBento client:load />
		</main>
	</body>
</html>
